<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= car.make %> <%= car.model %> - Details</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body { background: linear-gradient(135deg, #2e3b4e, #1c2530); color: #fff; }
    .car-details { padding: 2rem; }
    .car-image { width: 100%; height: auto; }
  </style>
</head>
<body>
  <div class="container car-details">
    <h1><%= car.make %> <%= car.model %> (<%= car.year %>)</h1>
    <img src="<%= car.imagePath || 'placeholder.jpg' %>" class="car-image mb-3" alt="<%= car.make %> <%= car.model %>">
    <p><strong>Fuel Type:</strong> <%= car.fuelType %></p>
    <p><strong>Price:</strong> $<%= car.price %></p>
    <p><strong>Mileage:</strong> <%= car.mileage %> miles</p>
    <p><strong>Description:</strong> <%= car.description %></p>
  </div>
</body>
</html>
